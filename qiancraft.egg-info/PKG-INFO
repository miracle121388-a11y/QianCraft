Metadata-Version: 2.4
Name: qiancraft
Version: 0.10.0
Summary: 持续维护在地文化与产品形态双库的自动文创设计系统
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: httpx>=0.28.1
Requires-Dist: json-repair>=0.44.0
Requires-Dist: numpy<2.3.0,>=2.0.0
Requires-Dist: Pillow<13.0.0,>=12.0.0
Requires-Dist: pydantic<3.0.0,>=2.11.0
Requires-Dist: python-dotenv>=1.1.0
Provides-Extra: test
Requires-Dist: pip-audit<3.0.0,>=2.10.1; extra == "test"
Requires-Dist: pytest>=8.4.0; extra == "test"
Requires-Dist: pytest-asyncio>=1.2.0; extra == "test"
Requires-Dist: ruff<0.17.0,>=0.16.5; extra == "test"

<p align="center">
  <img src="docs/assets/qiancraft-readme-hero.svg" width="100%" alt="QianCraft — 从文化证据与产品形态到可追溯设计">
</p>

<p align="center">
  <strong>持续维护在地文化与产品形态两座知识库，每天自动生成可追溯、可编辑的文创设计。</strong><br>
  <sub>A result-first, dual-library system for automated cultural product concepts.</sub>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/version-0.10.0-111111?style=flat-square" alt="Version 0.10.0">
  <img src="https://img.shields.io/badge/Python-%E2%89%A53.11%20%7C%20tested%203.13-315b7d?style=flat-square&logo=python&logoColor=white" alt="Python 3.11 or newer; tested on 3.13">
  <img src="https://img.shields.io/badge/tests-83%20Python%20%7C%205%20Web%20%7C%2032%20desktop%20UI-2f7358?style=flat-square" alt="83 Python tests, 5 Web tests and 32 desktop UI tests">
  <img src="https://img.shields.io/badge/local-0.10.0%20%7C%20online-0.9.2-b1844c?style=flat-square" alt="Local version 0.10.0; protected online version 0.9.2 pending deployment">
</p>

<p align="center">
  <a href="#产品逻辑">产品逻辑</a> ·
  <a href="#真实能力">真实能力</a> ·
  <a href="#快速启动">快速启动</a> ·
  <a href="#自动化">自动化</a> ·
  <a href="#可信边界">可信边界</a>
</p>

> [!IMPORTANT]
> QianCraft 当前止于概念设计、工厂询价和首样沟通。输出不是生产工程图、合规证书、社区/商业授权或“爆款保证”。

受保护的在线工具：[qiancraft-studio-2026.zeabur.app](https://qiancraft-studio-2026.zeabur.app)。入口启用 Basic Auth，凭证由项目维护者单独提供；密钥只由服务器 Secret 注入。此处先记录已验收的线上 0.9.2，0.10.0 部署完成后会以实际部署号、健康响应和业务 API 结果更新，不提前写成已上线。

## 产品逻辑

一个文创产品由“内容”和“形式”组成：

- **内容**来自当地真正有出处的文化、技艺、活动与生活方式，例如贵州苗绣、皮纸、村寨体育与地域叙事。
- **形式**来自全国市场已经出现信号的产品载体，例如冰箱贴、徽章、盲盒、包挂、香氛与毛绒。

QianCraft 不是把两边随意拼接，而是持续维护两座材料库，验证来源与样本，计算组合分数，每天选择最高分且文化/形态互不重复的最多 3 个组合，实际生成设计文件。

```text
公开文化来源 ──巡检/人工晋级──> 在地文化库 ──┐
                                                  ├─> 220 个组合候选
平台市场样本 ──授权采集/归一化──> 产品形态库 ──┘
                                                           │
                                              来源门 + 样本门 + 渲染器门
                                                           │
                                            每日 Top 3 / 手动自由组合
                                                           │
                                         PNG 概念稿 + 评分 + 来源 + 版本
                                                           │
                                  看中结果后才进入五阶段工作流编辑器
```

## 真实能力

| 能力 | 当前事实 |
|---|---|
| 在地文化库 | 22 条已核验正式记录、32 个登记来源；新发现只进入人工候选队列 |
| 产品形态库 | 10 种形态、378 条有时间边界的历史真实平台样本 |
| 组合空间 | 22 × 10 = 220 个可检查候选，后端公开五项分数与权重 |
| 每日产出 | 默认北京时间 07:00 选择最多 3 个通过门槛且互不重复的组合 |
| 设计文件 | 每次实际写入 1440 × 960 PNG，并记录版本、SHA-256、批次与生成方式 |
| 自由组合 | 选择 1–3 条文化内容和 1–3 个形态，生成后进入同一设计详情 |
| 结果编辑 | 替换/增加文化与形态，修改名称、受众、场景、文稿、要求和配色，生成 V2+ |
| 自动化 | 采集与每日设计各有持久线程、心跳、排程、事件和启动补跑 |
| 高级工作台 | 原九节点研究/设计空间保留在 `/workflow` |

当前自动 Top 3 来自真实库和公开规则。每天的具体结果、分数与设计 ID 由运行态产生，不硬编码在 README。

## 页面

| 路径 | 用途 |
|---|---|
| `/` | 今日设计、三项真实计数与自动化概览 |
| `/libraries/culture` | 查看文化内容、来源、转译空间和边界 |
| `/libraries/forms` | 查看形态排名、样本、平台覆盖和代表原记录 |
| `/create` | 自由选择多条文化与多种形态并立即生成 |
| `/designs` | 自动与手动设计档案 |
| `/designs/{id}` | 设计稿、评分、证据、谱系和生产前边界 |
| `/designs/{id}/edit` | 内容 → 形态 → 融合 → 视觉 → 生产前验证 |
| `/operations` | 调度线程、心跳、排程、阻断和事件 |
| `/workflow` | 旧高级空间工作台 |

## 快速启动

前置条件：Git、Python 3.11+（项目基线为 3.13）、Node.js 22 与 Corepack。仓库提供 Conda 环境和本地虚拟环境两种方式。

### 一条命令启动网页与 API

```bash
conda env create -f environment.yml
conda run --no-capture-output -n qiancraft python scripts/run_web_tool.py
```

默认：

- 网页：<http://localhost:3000/>
- API：<http://127.0.0.1:8787/>

若 3000 端口被其他项目占用：

```bash
conda run --no-capture-output -n qiancraft python scripts/run_web_tool.py --web-port 3001
```

也可以分别启动：

```bash
# Terminal 1
conda run --no-capture-output -n qiancraft python -m app.tool_api --port 8787

# Terminal 2
cd web
corepack enable
pnpm install --frozen-lockfile
pnpm dev --hostname 127.0.0.1 --port 3000
```

Tool API 启动后会同时启动持续采集和每日设计两个调度器，并在当天没有设计时执行补跑。前端只通过 HTTP API 读取数据，没有一套浏览器内假数据。

### 直接验证真实 API

```bash
curl http://127.0.0.1:8787/api/health
curl http://127.0.0.1:8787/api/studio/overview
curl http://127.0.0.1:8787/api/studio/libraries/culture
curl http://127.0.0.1:8787/api/studio/libraries/forms
curl http://127.0.0.1:8787/api/studio/combinations
```

## 自动化

默认配置：

```dotenv
QIANCRAFT_CONTINUOUS_COLLECTION=true
QIANCRAFT_CULTURE_WATCH_MINUTES=360
QIANCRAFT_MARKET_REFRESH_MINUTES=240
QIANCRAFT_DAILY_DESIGN_ENABLED=true
QIANCRAFT_DAILY_DESIGN_HOUR=7
QIANCRAFT_DAILY_DESIGN_MINUTE=0
```

运行语义：

- 文化巡检发现新网页后只创建 `pending_review` 候选，人工核验前不会改写 22 条正式记录。
- 市场刷新只有在实时开关、MediaCrawler 运行时和四个平台授权全部就绪时才执行。当前精简云端镜像缺少这些前置，因此会诚实显示 `blocked`。
- 378 条样本仍能作为有明确时间边界的历史证据维护产品形态库，但不能标成“今天实时抓取”。
- 每日设计独立于图像模型 provider，使用 10 个显式本地形态渲染器生成可检查的结构概念板；缺少某个形态渲染器会直接淘汰组合。
- 同日普通调度幂等；人工立即重跑会产生新批次并保留旧批次审计记录。
- `/api/health` 同时检查两个调度线程和心跳；异常返回 503，Docker 健康检查会交给平台重启策略恢复。

自动化状态和写入数据位于 `data/runtime/tool_workspace/collection/` 与 `data/runtime/tool_workspace/studio/`，Git 忽略它们；生产必须将 `/app/data/runtime` 挂载到持久卷。

## Studio API

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/studio/overview` | 首页、两库计数和自动化总览 |
| GET | `/api/studio/libraries/culture` | 文化正式库与来源 |
| GET | `/api/studio/libraries/forms` | 产品形态榜与代表样本 |
| GET | `/api/studio/combinations` | 可复现组合评分 |
| POST | `/api/studio/combinations` | 自由组合并生成设计 |
| GET | `/api/studio/designs` | 设计列表 |
| GET | `/api/studio/designs/{id}` | 单份设计详情 |
| PUT | `/api/studio/designs/{id}` | 修改并生成下一版本 |
| POST | `/api/studio/designs/{id}/regenerate` | 显式重生成下一版本 |
| GET | `/api/studio/automation/status` | 每日设计线程、心跳与排程 |
| GET | `/api/studio/automation/events` | 最近设计事件 |
| PUT | `/api/studio/automation/schedule` | 调整每日时间或暂停 |
| POST | `/api/studio/automation/run` | 立即重跑当天 Top 3 |

所有设计图由 `/assets/studio/{designId}/v{version}.png` 提供。

## 组合评分

`combo-score-v1`：

| 维度 | 权重 |
|---|---:|
| 文化证据 | 25% |
| 形态热度 | 25% |
| 品类兼容 | 25% |
| 转译空间 | 15% |
| 边界安全 | 10% |

评分只用于当前证据下的优先级，不是销售预测。自动 Top 3 还要求来源数 ≥ 2、存在可转译元素、形态样本 > 0、存在显式渲染器，并执行文化与形态去重。

## 验证

```bash
# Python
conda run --no-capture-output -n qiancraft python -m pytest -q
conda run --no-capture-output -n qiancraft ruff check .
conda run --no-capture-output -n qiancraft uv lock --check
conda run --no-capture-output -n qiancraft python -m pip_audit --local

# Web
cd web
pnpm test
pnpm typecheck
pnpm lint
pnpm audit --audit-level=high
pnpm build
```

GitHub Actions 在 Ubuntu、macOS 和 Windows 运行 Python 门，并在 Windows Chromium 执行前端构建与 32 项 desktop UI 门。浏览器自动化与 axe 结果不等于完整 WCAG 或真实设备认证。

## 部署

单服务 Docker 镜像运行 Nginx、Vinext 和 Python Tool API：

```bash
docker build -t qiancraft:0.10.0 .
```

Zeabur 必需配置：

- 持久卷：`/app/data/runtime`
- `QIANCRAFT_WEB_USERNAME` / `QIANCRAFT_WEB_PASSWORD`
- 可选 LLM 与图像 provider Secret
- 单 API 副本；多副本前必须增加分布式锁或外部队列

完整说明见 [docs/deployment_zeabur.md](docs/deployment_zeabur.md)。

## 可信边界

- 文化事实、来源、市场样本、评分和设计都可追溯；缺失条件不会用默认内容补成功。
- 本地生成的 PNG 是结构概念板，不是 AI 商品摄影、工程制图或打样照片。
- 未配置独立图像 provider 时，旧高级工作台的图像重生成仍显示 warning。
- 云端市场实时采集当前受上游运行时和四平台授权阻断；历史 378 条证据不会改名为 live。
- `reference_only` 馆藏图像不进入生成稿。
- 生产、商业图稿、文化授权、DFM、测试与合规审批始终留给真实责任人。

## 项目结构

```text
app/
  studio.py              # 双库读取、组合评分、PNG 生成、版本和每日调度
  collection.py          # 文化巡检与市场授权复检调度
  tool_api.py            # Studio + 旧 Workbench HTTP API
  workbench.py           # 原九节点高级工作台
web/app/
  studio-app.tsx         # 结果优先工具界面
  studio-api.ts          # Studio HTTP 客户端
  studio-model.ts        # 前端数据契约
  studio.css             # 新主界面样式
  workflow/              # 旧空间工作台路由
data/
  culture/               # 正式文化图谱
  market/derived/        # 形态榜与历史派生证据
  runtime/               # 本地/生产运行态，Git 忽略
tests/
  test_studio.py         # 双库、Top 3、版本、无兜底和调度回归
```

## 文档

- [产品契约](PRODUCT.md)
- [设计与交互契约](DESIGN.md)
- [架构](docs/architecture.md)
- [持续采集与每日生成](docs/continuous_collection.md)
- [Zeabur 部署](docs/deployment_zeabur.md)
- [项目工作流与更新台账](WORKFLOW.md)
- [来源与许可证](NOTICE.md)

## 许可证

QianCraft 自有代码按仓库 [LICENSE](LICENSE) 发布。提取的 LightRAG、GPT Researcher 与 MediaCrawler 目录保留其上游许可证和边界；MediaCrawler 的非商业学习使用限制不会因本项目封装而消失。
